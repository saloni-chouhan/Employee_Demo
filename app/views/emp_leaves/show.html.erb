<div class="container mt-3">
  <div class="mt-4 p-5 bg-light text-dark rounded">
    <h3>Department Detail</h3> 
    <p>
    <table class="table table-light table-striped table-hover">
    <thead>
      <tr>
        <th>From Date</th>
        <th>To Date</th>
        <th>Reason</th>
        <th>Mail To</th>
        <th>Employee Name</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><%= @emp_leave.from_date %></td>
        <td><%= @emp_leave.to_date %></td>
        <td><%= @emp_leave.reason %></td>
        <td><%= @emp_leave.mail_to %></td>
        <td><%= @emp_leave.employee.name %></td>
      </tr>
    </tbody>
  </table>
    <div class="text-center">
      <%= link_to 'Edit', edit_emp_leafe_path(@emp_leave), class: 'btn btn-outline-success' %>
      <%= link_to 'Back', emp_leaves_path, class: 'btn btn-outline-dark' %>
    </div>
  </p>
  </div>
</div>
